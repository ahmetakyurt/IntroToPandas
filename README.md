## Little Operations with Pandas DataFrames 

Initially, dataframes for worker group 1, and worker group 2.  
The program asks and receives an answer on whether the two dataframes should be merged or concatenated.
